import backgroundColor from "./colors/background-color";
import textColors from "./colors/text-color";
import statusColor from "./colors/status-color";
import { primaryButtons, secondaryButtons } from "./colors/buttons";
import fonts from "./fonts";

const theme = {
  backgroundColor,
  textColors,
  statusColor,
  primaryButtons,
  secondaryButtons,
  fonts,
};

export default theme;
